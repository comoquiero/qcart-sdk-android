pluginManagement {
    repositories {
        gradlePluginPortal()      // for Kotlin plugins
        google()                  // for Android Gradle plugin
        mavenCentral()
    }
}

rootProject.name = "deeplink-sdk"